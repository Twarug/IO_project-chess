{
  "tasks": [
    {
      "type": "cppbuild",
      "label": "Build Chess.exe",
      "command": "cl.exe",
      "args": [
        "/Zi",
        "/EHsc",
        "/nologo",
        "/std:c++20",
        "/Fe${workspaceFolder}/bin/Chess.exe",
        "${workspaceFolder}/*.cpp",
        "${workspaceFolder}/src/*.cpp"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [
        "$msCompile"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "Task generated by Debugger."
    }
  ],
  "version": "2.0.0"
}